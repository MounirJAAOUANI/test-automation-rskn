android {
    namespace "PACKAGE_ID_PLACEHOLDER"
    compileSdk 34

    defaultConfig {
        applicationId "PACKAGE_ID_PLACEHOLDER"
        minSdk 21
        targetSdk 34
        versionCode 1
        versionName "1.0"
    }

    buildTypes {
        release {
            signingConfig signingConfigs.debug
        }
    }
}

dependencies {
    implementation "org.jetbrains.kotlin:kotlin-stdlib-jdk7:$kotlin_version"
}